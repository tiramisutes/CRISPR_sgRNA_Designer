$schema: "http://json-schema.org/draft-06/schema#"

description: an entry in the sample sheet
properties:
  GeneID:
    type: string
    description: Gene id that in this genome file

required:
  - GeneID
